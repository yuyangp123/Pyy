import React from "react";
import { createRoot } from "react-dom/client";
import IntegratedReport from "./IntegratedReport.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <IntegratedReport />
  </React.StrictMode>
);
